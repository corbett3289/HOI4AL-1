sub_units = {
	# The vanilla carrier sub-unit requires its own hull archetype. Keep the
	# carrier combat role while accepting the Consensus mothership archetype.
	coi_alien_mothership = {
		# Namespaced sprite selects the matching GFX_unit_* roster icons and
		# coi_alien_mothership_entity without changing any human navy art.
		sprite = coi_alien_mothership
		map_icon_category = ship
		priority = 10
		active = yes
		type = { carrier }
		need = { coi_alien_mothership_hull = 1 }

		supply_consumption = 0.8

		max_organisation = 40

		critical_parts = { destroyed_ammo_storage broken_propeller on_fire damaged_flight_deck }
		critical_part_damage_chance_mult = 1

		hit_profile_mult = 1.0
	}

	# The vanilla destroyer sub-unit requires its own hull archetype. This
	# definition preserves its screen role but consumes the alien escort hull.
	coi_alien_escort = {
		# Namespaced sprite selects the matching GFX_unit_* roster icons and
		# coi_alien_escort_entity without changing any human navy art.
		sprite = coi_alien_escort
		map_icon_category = ship
		priority = 3
		active = yes
		type = { screen_ship }
		need = { coi_alien_escort_hull = 1 }

		max_organisation = 35

		supply_consumption = 0.04

		critical_parts = { destroyed_ammo_storage broken_propeller on_fire rudder_jammed }
		critical_part_damage_chance_mult = 1

		hit_profile_mult = 1.0
	}
}
